class PianoSampler {
  constructor() {
    this.playNote = (midiNum, velocity) => {
      return 0;
    };
    this.stopNote = (midiNum) => {
      return 0;
    };
  }
}

export default new PianoSampler();
