import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ChordTableMode = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default ChordTableMode;

const styles = StyleSheet.create({});
